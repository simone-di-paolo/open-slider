import React from 'react';
import { createRoot } from 'react-dom/client';

import OpenSlider from './components/OpenSlider';

const root = createRoot(document.getElementById('root'));
root.render(<OpenSlider />);